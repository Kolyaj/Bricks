- [ ] 20260921-7 `DOM.normalizeCSSProperty`: seam (явный etalon) для тестируемости больше не требуется (ADR 0003 — etalon уже параметр `el`, в Node глобалы контролируются); остаётся продуктовое решение о скрытой `document.documentElement`-зависимости без `el`
- [ ] 20260924-1 `Sound` — синглтон без `new` (контракт инстанцирования: потребитель не может создать экземпляр с другим doc) — продуктовое решение
- [ ] 20260924-2 `Rnd.initByArray` — вызов без `keyLength` тихо пропускает смешивание ключа (`k = this.N > keyLength ? … : keyLength` → undefined → первый цикл не выполняется; исходный gist брал `init_key.length` сам) — продуктовое решение: выводить keyLength из `initKey.length` или задокументировать обязательность аргумента
