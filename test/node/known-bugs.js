// Реестр известных багов — known-red гейт (ADR 0003).
//
// Тест известного бага несёт тег [bug <todo-id>] в заголовке it.
// Упавший тест, чей тег есть в этом реестре, не роняет `npm test`
// (в выводе остаётся красным — прогресс виден), но тест с неизвестным
// тегом или вообще без тега роняет сьюит.
//
// После фикса бага: удалить запись из реестра и тег из заголовка it —
// тест остаётся и работает как обычный регрессионный.

module.exports = {};
