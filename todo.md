- [ ] 20260921-2 Баг: `Bricks.Date.getDayOfYear` вызывает `this.getDate()` (this = Bricks.Date), а не `date.getDate()` — lib/Bricks/Date.js — взорвётся при первом вызове; фикс вместе с созданием теста (батч 1)
- [ ] 20260921-3 Баг: `Bricks.JSON.parse` фолбэк `new Function('return ' + json)()` — eval ответа сервера (code injection) + `window` при загрузке — lib/Bricks/JSON.js; фикс вместе с созданием теста (батч 1)
- [ ] 20260921-5 Баг: `Bricks.Function.throttle` — `arguments.callee` в IIFE — throw в strict-бандле — lib/Bricks/Function.js; фикс вместе с созданием теста (батч 1)
- [ ] 20260921-6 Баг: `Bricks.Cookie.readCookie` опирается на `RegExp.$1` (состояние последнего match) — хрупкое implicit-global — lib/Bricks/Cookie.js; фикс вместе с созданием теста (батч 3, браузер)
- [ ] 20260921-7 `DOM.normalizeCSSProperty`: seam (явный etalon) для тестируемости больше не требуется (ADR 0003 — etalon уже параметр `el`, в Node глобалы контролируются); остаётся продуктовое решение о скрытой `document.documentElement`-зависимости без `el`
- [ ] 20260921-9 Тест-батч 1 (ADR 0003): чистые модули в Node — String, Number (`--set lang_ru` + `lang_en`), Date, Rnd, QueryString, Function, index-остаток (mixin/getPrototypeChain/Values/range/isArray), Array.isArray, JSON, Observer, Event, EventsController (профили событий) + перенос существующих браузерных тестов
- [ ] 20260921-10 Тест-батч 2 (ADR 0003): нормализаторы через браузерные профили — DOM-листья (isAncestor, class*, getWindow, getDocumentScroll/Size, getViewportSize, getPos, normalizeCSSProperty/Value), Sound (детект audio/bgsound), Remote (XHR-профили), DragController (fake-события) + conformance-meta-тесты профилей
- [ ] 20260921-11 Тест-батч 3 (ADR 0003): browser-минимум в Yaxy (Cookie, DOM-дерево: getEl/getEls/createSelectorFilter/remove/createFragment/setStyle/on/un/initDrag, Sound._createAudioEl), синхронизация AGENTS.md/ADR
